# --- Database ---
POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme_in_production
POSTGRES_DB=payment_recon
POSTGRES_PORT=5433

# --- App ---
RECON_DATABASE_URL=postgresql://postgres:changeme_in_production@db:5432/payment_recon
APP_PORT=8000

# --- Reconciliation tolerances (all optional — defaults shown) ---
# RECON_ROUNDING_TOLERANCE=0.02
# RECON_FEE_TOLERANCE=0.05
# RECON_SETTLEMENT_DELAY_DAYS=5

# --- AI (optional — endpoint degrades gracefully without it) ---
ANTHROPIC_API_KEY=sk-ant-your-key-here
